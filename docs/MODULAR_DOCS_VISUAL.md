# 📊 Modular Documentation System - Visual Guide

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Your Documentation Files                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ auth.json    │  │ users.json   │  │therapists.json│         │
│  │              │  │              │  │              │         │
│  │ • register   │  │ • GET all    │  │ • profile    │         │
│  │ • login      │  │ • GET :id    │  │ • GET :id    │         │
│  │ • logout     │  │ • PUT        │  │ • GET all    │         │
│  │ • refresh    │  │   profile    │  │ • availability│         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│         │                 │                  │                  │
│         └─────────────────┼──────────────────┘                  │
│                           ↓                                     │
│              ┌─────────────────────────┐                        │
│              │  api.base.json          │                        │
│              │  • API info             │                        │
│              │  • Server URLs (5000)   │                        │
│              │  • Tags                 │                        │
│              │  • Common schemas       │                        │
│              └─────────────────────────┘                        │
└─────────────────────────────────────────────────────────────────┘
                           ↓
          ┌─────────────────────────────────────┐
          │   consolidate-docs.js               │
          │                                     │
          │   1. Read api.base.json             │
          │   2. Read all feature files         │
          │   3. Merge paths & schemas          │
          │   4. Write to api.json              │
          └─────────────────────────────────────┘
                           ↓
          ┌─────────────────────────────────────┐
          │   api.json                          │
          │   (Auto-generated - 1486 lines)     │
          │   ✅ 14 endpoints                   │
          │   ✅ 4 schemas                      │
          │   ✅ Port 5000                      │
          └─────────────────────────────────────┘
                           ↓
          ┌─────────────────────────────────────┐
          │   docs.routes.js                    │
          │   Loads api.json                    │
          │   Passes to Scalar                  │
          └─────────────────────────────────────┘
                           ↓
          ┌─────────────────────────────────────┐
          │   Scalar API Reference              │
          │   Beautiful Interactive Docs        │
          │   http://localhost:5000/docs        │
          └─────────────────────────────────────┘
```

---

## 🔄 Editing Workflow

```
┌──────────────────────────────────────────────────────────┐
│  Step 1: Edit Feature File                              │
│  ────────────────────────────────────────────────        │
│                                                          │
│  You edit:  src/docs/auth.json                           │
│             src/docs/users.json                          │
│             src/docs/therapists.json                     │
│                                                          │
│  ✏️  Add/modify endpoints                                │
│  ✏️  Update request/response schemas                     │
│  ✏️  Add examples                                        │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│  Step 2: Run Consolidation                              │
│  ────────────────────────────────────────────────        │
│                                                          │
│  Command: npm run docs:consolidate                       │
│                                                          │
│  Script reads all files and merges them                  │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│  Step 3: api.json Updated                               │
│  ────────────────────────────────────────────────        │
│                                                          │
│  ✅ All endpoints consolidated                           │
│  ✅ All schemas merged                                   │
│  ✅ Ready for Scalar                                     │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│  Step 4: Refresh Browser                                │
│  ────────────────────────────────────────────────        │
│                                                          │
│  Visit: http://localhost:5000/docs                       │
│  Your changes appear immediately!                        │
└──────────────────────────────────────────────────────────┘
```

---

## 📁 File Relationships

```
api.base.json (Template)
    │
    ├─── Contains:
    │    • API metadata (title, version, description)
    │    • Server URLs (http://localhost:5000)
    │    • Tags (Health, Auth, Users, Therapists)
    │    • Common schemas (User, ApiResponse, ApiError)
    │    • Security schemes (bearerAuth, cookieAuth)
    │
    ↓
    
auth.json (Authentication)
    │
    ├─── Contains:
    │    • POST /api/v1/auth/register
    │    • POST /api/v1/auth/login
    │    • POST /api/v1/auth/logout
    │    • POST /api/v1/auth/refresh-token
    │
    ↓
    
users.json (User Management)
    │
    ├─── Contains:
    │    • GET /api/v1/users
    │    • GET /api/v1/users/:id
    │    • PUT /api/v1/users/profile
    │
    ↓
    
therapists.json (Therapist Features)
    │
    ├─── Contains:
    │    • POST /api/v1/therapists/profile
    │    • PUT /api/v1/therapists/profile
    │    • GET /api/v1/therapists/:id
    │    • GET /api/v1/therapists
    │    • PUT /api/v1/therapists/availability
    │    • PUT /api/v1/therapists/qualifications
    │    • PUT /api/v1/therapists/specializations
    │    • Therapist schema
    │
    ↓
    
consolidate-docs.js merges all ↓

api.json (Consolidated)
    │
    └─── Used by Scalar to generate docs
```

---

## 🎯 What Goes Where?

```
┌──────────────────────────────────────────────────────────┐
│  api.base.json                                           │
├──────────────────────────────────────────────────────────┤
│  ✅ API title, version, description                      │
│  ✅ Server URLs (development, production)                │
│  ✅ Tags (categorize endpoints)                          │
│  ✅ Common schemas (User, ApiResponse, ApiError)         │
│  ✅ Security schemes (JWT auth)                          │
│  ✅ /health endpoint (always available)                  │
│                                                          │
│  ❌ Feature-specific endpoints                           │
│  ❌ Feature-specific schemas                             │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  auth.json                                               │
├──────────────────────────────────────────────────────────┤
│  ✅ All /api/v1/auth/* endpoints                         │
│  ✅ Authentication-related paths                         │
│                                                          │
│  ❌ User management endpoints                            │
│  ❌ Therapist endpoints                                  │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  users.json                                              │
├──────────────────────────────────────────────────────────┤
│  ✅ All /api/v1/users/* endpoints                        │
│  ✅ User management paths                                │
│                                                          │
│  ❌ Authentication endpoints                             │
│  ❌ Therapist-specific endpoints                         │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  therapists.json                                         │
├──────────────────────────────────────────────────────────┤
│  ✅ All /api/v1/therapists/* endpoints                   │
│  ✅ Therapist schema (extends User)                      │
│  ✅ Therapist-specific functionality                     │
│                                                          │
│  ❌ General user endpoints                               │
│  ❌ Authentication endpoints                             │
└──────────────────────────────────────────────────────────┘
```

---

## 🆕 Adding New Feature - Step by Step

```
Step 1: Create File
┌────────────────────────────────┐
│ src/docs/sessions.json         │
│                                │
│ {                              │
│   "paths": { ... }             │
│   "components": {              │
│     "schemas": { ... }         │
│   }                            │
│ }                              │
└────────────────────────────────┘
         ↓
Step 2: Register File
┌────────────────────────────────┐
│ consolidate-docs.js            │
│                                │
│ const FEATURE_FILES = [        │
│   'auth.json',                 │
│   'users.json',                │
│   'therapists.json',           │
│   'sessions.json'  ← Add this  │
│ ];                             │
└────────────────────────────────┘
         ↓
Step 3: Add Tag
┌────────────────────────────────┐
│ api.base.json                  │
│                                │
│ "tags": [                      │
│   { "name": "Health" },        │
│   { "name": "Authentication" },│
│   { "name": "Users" },         │
│   { "name": "Therapists" },    │
│   { "name": "Sessions" } ← Add │
│ ]                              │
└────────────────────────────────┘
         ↓
Step 4: Consolidate
┌────────────────────────────────┐
│ npm run docs:consolidate       │
└────────────────────────────────┘
         ↓
Step 5: Done! ✅
┌────────────────────────────────┐
│ http://localhost:5000/docs     │
│ New endpoints visible!         │
└────────────────────────────────┘
```

---

## 📊 Current Statistics

```
┌─────────────────────────────────────────────┐
│  Documentation Status                       │
├─────────────────────────────────────────────┤
│  📁 Feature Files: 3                        │
│     • auth.json                             │
│     • users.json                            │
│     • therapists.json                       │
│                                             │
│  📝 Total Endpoints: 14                     │
│     • Health: 1                             │
│     • Authentication: 4                     │
│     • Users: 3                              │
│     • Therapists: 7                         │
│                                             │
│  🗂️  Total Schemas: 4                       │
│     • User                                  │
│     • ApiResponse                           │
│     • ApiError                              │
│     • Therapist                             │
│                                             │
│  🌐 Server: http://localhost:5000           │
│  📖 Docs: http://localhost:5000/docs        │
└─────────────────────────────────────────────┘
```

---

## 🔧 NPM Scripts Flow

```
npm run dev
    │
    ├─── Starts development server
    ├─── MongoDB connects
    ├─── Server runs on port 5000
    └─── Docs available at /docs
    
npm run docs:consolidate
    │
    ├─── Runs consolidate-docs.js
    ├─── Reads api.base.json
    ├─── Reads all feature files
    ├─── Merges everything
    └─── Writes api.json
    
npm run docs:watch
    │
    ├─── Watches *.json files
    ├─── Auto-runs consolidate on change
    └─── No manual consolidation needed!
```

---

## 🎨 Benefits Visualization

```
BEFORE (Single File)                AFTER (Modular)
═══════════════════                 ═══════════════

api.json (2000+ lines)              api.base.json (100 lines)
├─ Health endpoints                 auth.json (300 lines)
├─ Auth endpoints                   users.json (200 lines)
├─ User endpoints                   therapists.json (400 lines)
├─ Therapist endpoints              ↓ consolidate ↓
├─ Session endpoints                api.json (auto-generated)
└─ All schemas

❌ Hard to navigate                 ✅ Easy to find things
❌ Merge conflicts                  ✅ Team-friendly
❌ Difficult to maintain            ✅ Simple to update
❌ No organization                  ✅ Well organized
```

---

## 🚀 Quick Reference

| Action | Command |
|--------|---------|
| Edit auth endpoints | `vim src/docs/auth.json` |
| Edit user endpoints | `vim src/docs/users.json` |
| Edit therapist endpoints | `vim src/docs/therapists.json` |
| Consolidate docs | `npm run docs:consolidate` |
| Auto-consolidate | `npm run docs:watch` |
| View docs | http://localhost:5000/docs |
| Start server | `npm run dev` |

---

**🎊 Your modular documentation system is ready!**

```
        Edit → Consolidate → Refresh
           ↑__________________|
                 Repeat!
```
